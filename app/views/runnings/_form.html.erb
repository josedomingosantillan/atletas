<%= form_for(running) do |f| %>
  <% if running.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(running.errors.count, "error") %> prohibited this running from being saved:</h2>

      <ul>
      <% running.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :one_lap %>
    <%= f.text_field :one_lap %>
  </div>

  <div class="field">
    <%= f.label :two_lap %>
    <%= f.text_field :two_lap %>
  </div>

  <div class="field">
    <%= f.label :three_lap %>
    <%= f.text_field :three_lap %>
  </div>

  <div class="field">
    <%= f.label :total %>
    <%= f.text_field :total %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
